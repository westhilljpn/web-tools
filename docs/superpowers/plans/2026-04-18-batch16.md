# Batch 16 — 6ツール追加 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** B16の6ツールを追加し、B15のtoolComponentsバグを修正する

**Architecture:** 各ツールは`src/tools/[Component].tsx`に実装。`src/lib/toolsRegistry.ts`と`src/lib/toolComponents.tsx`に登録。翻訳ファイルをja/en別に`src/messages/[locale]/tools/[slug].json`に配置。TextSorter.tsxのパターンに準拠。

**Tech Stack:** Next.js 14 App Router, TypeScript strict, Tailwind CSS, next-intl 4.x, DataView API (exif-viewer), Intl.NumberFormat (number-formatter), localStorage (countdown-timer)

---

### Task 0: B15 toolComponents バグ修正

**Files:**
- Modify: `src/lib/toolComponents.tsx` (行69: ReadingTime の後に追加)

- [ ] **Step 1: toolComponents.tsx に B15 の 6 エントリを追加**

`ReadingTime` の行の直後に以下を追加:

```tsx
  DateCalculator: dynamic(() => import("@/tools/DateCalculator"), { loading: Loading }),
  CalorieCalculator: dynamic(() => import("@/tools/CalorieCalculator"), { loading: Loading }),
  BodyFatCalculator: dynamic(() => import("@/tools/BodyFatCalculator"), { loading: Loading }),
  SleepCalculator: dynamic(() => import("@/tools/SleepCalculator"), { loading: Loading }),
  CssBoxShadow: dynamic(() => import("@/tools/CssBoxShadow"), { loading: Loading }),
  WcagContrastChecker: dynamic(() => import("@/tools/WcagContrastChecker"), { loading: Loading }),
```

- [ ] **Step 2: ビルド確認**

```bash
npm run build
```

Expected: ✓ Compiled successfully

- [ ] **Step 3: コミット**

```bash
git add src/lib/toolComponents.tsx
git commit -m "[修正] toolComponents.tsx にB15の6コンポーネントを追加（Coming Soon解消）"
```

---

### Task 1: text-deduplicator — 重複行削除ツール

**Files:**
- Create: `src/tools/TextDeduplicator.tsx`
- Create: `src/messages/ja/tools/text-deduplicator.json`
- Create: `src/messages/en/tools/text-deduplicator.json`
- Modify: `src/lib/toolsRegistry.ts` (末尾 wcag-contrast-checker の後)
- Modify: `src/lib/toolComponents.tsx` (末尾 WcagContrastChecker の後)

- [ ] **Step 1: コンポーネント作成**

`src/tools/TextDeduplicator.tsx`:

```tsx
"use client";

import { useState, useMemo } from "react";
import { useTranslations } from "next-intl";

export default function TextDeduplicator() {
  const t = useTranslations("text-deduplicator");
  const [input, setInput] = useState("");
  const [caseSensitive, setCaseSensitive] = useState(false);
  const [trimWhitespace, setTrimWhitespace] = useState(true);
  const [removeEmpty, setRemoveEmpty] = useState(false);
  const [copied, setCopied] = useState(false);

  const { output, inputCount, outputCount, removedCount } = useMemo(() => {
    let lines = input.split("\n");
    if (removeEmpty) lines = lines.filter((l) => l.trim() !== "");
    const seen = new Set<string>();
    const result: string[] = [];
    for (const line of lines) {
      const trimmed = trimWhitespace ? line.trim() : line;
      const key = caseSensitive ? trimmed : trimmed.toLowerCase();
      if (!seen.has(key)) {
        seen.add(key);
        result.push(trimmed);
      }
    }
    return {
      output: result.join("\n"),
      inputCount: lines.length,
      outputCount: result.length,
      removedCount: lines.length - result.length,
    };
  }, [input, caseSensitive, trimWhitespace, removeEmpty]);

  async function handleCopy() {
    await navigator.clipboard.writeText(output);
    setCopied(true);
    setTimeout(() => setCopied(false), 2000);
  }

  return (
    <div className="space-y-4">
      {/* オプション */}
      <div className="flex flex-wrap gap-5 text-sm">
        {(
          [
            { val: caseSensitive, set: setCaseSensitive, key: "caseSensitive" },
            { val: trimWhitespace, set: setTrimWhitespace, key: "trimWhitespace" },
            { val: removeEmpty, set: setRemoveEmpty, key: "removeEmpty" },
          ] as const
        ).map(({ val, set, key }) => (
          <label key={key} className="flex items-center gap-2 cursor-pointer select-none">
            <input
              type="checkbox"
              checked={val}
              onChange={(e) => set(e.target.checked)}
              className="w-4 h-4 accent-primary"
            />
            <span className="text-gray-700">{t(`options.${key}`)}</span>
          </label>
        ))}
      </div>

      {/* 統計バッジ */}
      {input && (
        <div className="flex flex-wrap gap-2 text-xs">
          <span className="px-3 py-1 rounded-full bg-blue-50 text-blue-700">
            {t("stats.input")} {inputCount}
          </span>
          <span className="px-3 py-1 rounded-full bg-green-50 text-green-700">
            {t("stats.output")} {outputCount}
          </span>
          {removedCount > 0 && (
            <span className="px-3 py-1 rounded-full bg-red-50 text-red-700">
              {t("stats.removed")} {removedCount}
            </span>
          )}
        </div>
      )}

      {/* 入力 / 出力 */}
      <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
        <div>
          <div className="flex items-center justify-between mb-1.5">
            <label className="text-sm font-medium text-gray-700">{t("inputLabel")}</label>
            {input && (
              <button
                type="button"
                onClick={() => setInput("")}
                className="text-xs text-gray-400 hover:text-gray-600"
              >
                {t("clear")}
              </button>
            )}
          </div>
          <textarea
            value={input}
            onChange={(e) => setInput(e.target.value)}
            placeholder={t("placeholder")}
            rows={12}
            className="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm font-mono resize-y
                       focus:outline-none focus:ring-2 focus:ring-primary/30 focus:border-primary"
          />
        </div>

        <div>
          <div className="flex items-center justify-between mb-1.5">
            <label className="text-sm font-medium text-gray-700">{t("outputLabel")}</label>
            <button
              type="button"
              onClick={handleCopy}
              disabled={!output}
              className="text-xs px-3 py-1 rounded bg-gray-200 hover:bg-gray-300 text-gray-700
                         disabled:opacity-40 transition-colors"
            >
              {copied ? t("copied") : t("copy")}
            </button>
          </div>
          <textarea
            value={output}
            readOnly
            rows={12}
            className="w-full px-3 py-2 border border-gray-200 rounded-lg text-sm font-mono resize-y
                       bg-gray-50 dark:bg-slate-800/50"
          />
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 日本語翻訳ファイル作成**

`src/messages/ja/tools/text-deduplicator.json`:

```json
{
  "title": "重複行削除ツール",
  "description": "テキストの重複した行を並び替えなしで削除。元の順序を保ちながら大文字小文字の区別・空白トリムに対応した無料オンラインツール。",
  "keywords": [
    "重複行 削除",
    "行 重複 除去 オンライン",
    "テキスト 重複削除 無料",
    "リスト 重複 取り除く",
    "重複行 一括削除"
  ],
  "inputLabel": "入力テキスト",
  "outputLabel": "重複削除後",
  "placeholder": "1行ずつテキストを入力またはペーストしてください...",
  "clear": "クリア",
  "copy": "コピー",
  "copied": "コピー済み",
  "options": {
    "caseSensitive": "大文字・小文字を区別する",
    "trimWhitespace": "前後の空白をトリムする",
    "removeEmpty": "空行を削除する"
  },
  "stats": {
    "input": "入力:",
    "output": "出力:",
    "removed": "削除:"
  },
  "howToUse": [
    {
      "label": "テキストを入力する",
      "description": "左のエリアに重複が含まれるテキストを1行ずつ貼り付けます。名簿・URLリスト・コードの変数名など幅広い用途に使えます。"
    },
    {
      "label": "オプションを設定する",
      "description": "大文字小文字の区別・前後空白のトリム・空行削除を組み合わせて設定します。デフォルトでは空白トリムがオンになっています。"
    },
    {
      "label": "結果をコピーする",
      "description": "右のエリアに重複が除去されたテキストが表示されます。元の順序は保たれます。コピーボタンでクリップボードに貼り付けられます。"
    }
  ],
  "faq": [
    {
      "question": "重複行を削除して元の順番を維持したい",
      "answer": "このツールは元の行の順序を保ったまま重複を削除します。最初に出現した行を残し、2回目以降の同じ内容の行を除去します。ソートが不要な場合はこのツールが適しています。"
    },
    {
      "question": "大文字と小文字を同じ行として扱えますか？",
      "answer": "はい。「大文字・小文字を区別する」をオフにすると、「Apple」「apple」「APPLE」はすべて同じ行と見なされます。デフォルトはオフです。"
    },
    {
      "question": "行の前後にスペースがある場合は？",
      "answer": "「前後の空白をトリムする」をオンにすると、各行の先頭・末尾のスペースやタブを除去してから重複判定します。「　apple　」と「apple」を同一とみなすことができます。"
    },
    {
      "question": "テキスト行並び替えツールとの違いは？",
      "answer": "テキスト行並び替えツールはソートと同時に重複削除を行います。このツールはソートを行わず、元の順序を保ったまま重複のみを削除します。順序を変えたくない場合はこのツールをお使いください。"
    },
    {
      "question": "何行まで処理できますか？",
      "answer": "ブラウザ上で動作するため、数万行程度まで問題なく処理できます。データが非常に大きい場合はブラウザが一時的に遅くなることがありますが、サーバーへの送信は一切行いません。"
    }
  ]
}
```

- [ ] **Step 3: 英語翻訳ファイル作成**

`src/messages/en/tools/text-deduplicator.json`:

```json
{
  "title": "Text Deduplicator",
  "description": "Remove duplicate lines from text while preserving original order. Supports case-insensitive matching and whitespace trimming. Free online tool.",
  "keywords": [
    "remove duplicate lines",
    "deduplicate text online",
    "unique lines tool",
    "text deduplication free",
    "remove repeated lines"
  ],
  "inputLabel": "Input Text",
  "outputLabel": "Deduplicated Output",
  "placeholder": "Paste text with duplicate lines here...",
  "clear": "Clear",
  "copy": "Copy",
  "copied": "Copied!",
  "options": {
    "caseSensitive": "Case sensitive",
    "trimWhitespace": "Trim whitespace",
    "removeEmpty": "Remove empty lines"
  },
  "stats": {
    "input": "Input:",
    "output": "Output:",
    "removed": "Removed:"
  },
  "howToUse": [
    {
      "label": "Paste your text",
      "description": "Paste the text with duplicate lines into the left area. Works great for lists, URLs, log entries, and more."
    },
    {
      "label": "Configure options",
      "description": "Choose whether to use case-sensitive matching, trim whitespace, or remove empty lines."
    },
    {
      "label": "Copy the result",
      "description": "The deduplicated text appears on the right, preserving the original order. Click Copy to copy to clipboard."
    }
  ],
  "faq": [
    {
      "question": "Does this preserve the original line order?",
      "answer": "Yes. This tool keeps lines in their original order, retaining the first occurrence of each duplicate and removing subsequent ones. Use text-sorter if you also need to sort."
    },
    {
      "question": "Can I treat uppercase and lowercase as the same?",
      "answer": "Yes. Uncheck 'Case sensitive' to treat 'Apple', 'apple', and 'APPLE' as duplicates. The default is case-insensitive."
    },
    {
      "question": "What does 'Trim whitespace' do?",
      "answer": "When enabled, leading and trailing spaces are removed from each line before comparison, so '  apple  ' and 'apple' are treated as duplicates."
    },
    {
      "question": "How is this different from the Text Sorter?",
      "answer": "Text Sorter sorts lines and optionally removes duplicates. This tool removes duplicates without sorting, preserving the original order."
    },
    {
      "question": "How many lines can it handle?",
      "answer": "The tool runs entirely in your browser and can handle tens of thousands of lines without any issues. No data is sent to a server."
    }
  ]
}
```

- [ ] **Step 4: toolsRegistry.ts に追加**

`wcag-contrast-checker` エントリの直後に追加:

```ts
  {
    slug: "text-deduplicator",
    category: "text",
    icon: "🧹",
    component: "TextDeduplicator",
    updatedAt: "2026-04-18",
  },
```

- [ ] **Step 5: toolComponents.tsx に追加**

`WcagContrastChecker` の直後に追加:

```tsx
  TextDeduplicator: dynamic(() => import("@/tools/TextDeduplicator"), { loading: Loading }),
```

- [ ] **Step 6: ビルド確認**

```bash
npm run build
```

Expected: ✓ Compiled successfully (新しいツールページが生成される)

- [ ] **Step 7: コミット**

```bash
git add src/tools/TextDeduplicator.tsx \
        src/messages/ja/tools/text-deduplicator.json \
        src/messages/en/tools/text-deduplicator.json \
        src/lib/toolsRegistry.ts \
        src/lib/toolComponents.tsx
git commit -m "[ツール追加] text-deduplicator: 重複行削除ツール"
```

---

### Task 2: character-counter-jp — 原稿用紙換算ツール

**Files:**
- Create: `src/tools/CharacterCounterJp.tsx`
- Create: `src/messages/ja/tools/character-counter-jp.json`
- Create: `src/messages/en/tools/character-counter-jp.json`
- Modify: `src/lib/toolsRegistry.ts`
- Modify: `src/lib/toolComponents.tsx`

- [ ] **Step 1: コンポーネント作成**

`src/tools/CharacterCounterJp.tsx`:

```tsx
"use client";

import { useMemo, useState } from "react";
import { useTranslations } from "next-intl";

const GENKOU_SIZE = 400;
const TWITTER_LIMIT = 280;

function isHalfWidth(ch: string): boolean {
  const cp = ch.codePointAt(0) ?? 0;
  return (cp >= 0x00 && cp <= 0x7f) || (cp >= 0xff61 && cp <= 0xff9f);
}

function countTwitterWeight(text: string): number {
  let count = 0;
  for (const ch of text) {
    const cp = ch.codePointAt(0) ?? 0;
    count += cp >= 0x1100 && cp <= 0xffef ? 2 : 1;
  }
  return count;
}

export default function CharacterCounterJp() {
  const t = useTranslations("character-counter-jp");
  const [input, setInput] = useState("");

  const stats = useMemo(() => {
    const chars = [...input];
    const total = chars.length;
    const withoutNewlines = chars.filter((c) => c !== "\n").length;
    const half = chars.filter((c) => isHalfWidth(c) && c !== "\n").length;
    const full = withoutNewlines - half;
    const twitterWeight = countTwitterWeight(input.replace(/\n/g, ""));
    return { total, withoutNewlines, half, full, twitterWeight };
  }, [input]);

  const genkouPages = stats.total / GENKOU_SIZE;
  const genkouBarPct = Math.min(((genkouPages % 1) || (genkouPages > 0 ? 1 : 0)) * 100, 100);
  const twitterRemaining = TWITTER_LIMIT - stats.twitterWeight;
  const twitterBarPct = Math.min((stats.twitterWeight / TWITTER_LIMIT) * 100, 100);
  const twitterOver = twitterRemaining < 0;

  const RESUME_SECTIONS = [
    { key: "motivation", min: 100, max: 300 },
    { key: "selfPr", min: 100, max: 300 },
    { key: "activities", min: 100, max: 200 },
  ] as const;

  return (
    <div className="space-y-6">
      <textarea
        value={input}
        onChange={(e) => setInput(e.target.value)}
        placeholder={t("placeholder")}
        rows={8}
        className="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm resize-y
                   focus:outline-none focus:ring-2 focus:ring-primary/30 focus:border-primary"
      />

      {/* 基本統計 */}
      <div className="grid grid-cols-2 sm:grid-cols-4 gap-3">
        {(
          [
            { key: "total", val: stats.total },
            { key: "withoutNewlines", val: stats.withoutNewlines },
            { key: "halfWidth", val: stats.half },
            { key: "fullWidth", val: stats.full },
          ] as const
        ).map(({ key, val }) => (
          <div key={key} className="tool-card p-3 text-center">
            <div className="text-2xl font-bold text-primary tabular-nums">
              {val.toLocaleString()}
            </div>
            <div className="text-xs text-gray-500 mt-1">{t(`stats.${key}`)}</div>
          </div>
        ))}
      </div>

      {/* 原稿用紙換算 */}
      <div className="tool-card p-4">
        <h2 className="text-sm font-semibold text-gray-700 mb-3">{t("sections.genkou")}</h2>
        <div className="flex items-end gap-2 mb-2">
          <span className="text-3xl font-bold text-primary tabular-nums">
            {genkouPages.toFixed(1)}
          </span>
          <span className="text-sm text-gray-500 mb-1">{t("genkouUnit")}</span>
        </div>
        <p className="text-xs text-gray-400 mb-2">{t("genkouNote")}</p>
        <div className="h-2 bg-gray-100 rounded-full overflow-hidden">
          <div
            className="h-full bg-primary/60 rounded-full transition-all duration-300"
            style={{ width: `${genkouBarPct}%` }}
          />
        </div>
      </div>

      {/* X/Twitter */}
      <div className="tool-card p-4">
        <h2 className="text-sm font-semibold text-gray-700 mb-3">{t("sections.twitter")}</h2>
        <div className="flex items-center justify-between mb-2">
          <span className="tabular-nums font-medium text-sm">
            {stats.twitterWeight} / {TWITTER_LIMIT}
          </span>
          <span
            className={`text-sm font-medium tabular-nums ${
              twitterOver ? "text-red-500" : "text-gray-600"
            }`}
          >
            {twitterOver
              ? `${Math.abs(twitterRemaining)} ${t("over")}`
              : `${twitterRemaining} ${t("remaining")}`}
          </span>
        </div>
        <div className="h-2 bg-gray-100 rounded-full overflow-hidden">
          <div
            className={`h-full rounded-full transition-all duration-300 ${
              twitterOver ? "bg-red-400" : "bg-primary/60"
            }`}
            style={{ width: `${Math.min(twitterBarPct, 100)}%` }}
          />
        </div>
        <p className="text-xs text-gray-400 mt-2">{t("twitterNote")}</p>
      </div>

      {/* 履歴書目安 */}
      <div className="tool-card p-4">
        <h2 className="text-sm font-semibold text-gray-700 mb-3">{t("sections.resume")}</h2>
        <div className="space-y-4">
          {RESUME_SECTIONS.map(({ key, min, max }) => {
            const pct = Math.min((stats.withoutNewlines / max) * 100, 100);
            const inRange =
              stats.withoutNewlines >= min && stats.withoutNewlines <= max;
            const over = stats.withoutNewlines > max;
            return (
              <div key={key}>
                <div className="flex justify-between text-xs text-gray-500 mb-1">
                  <span>{t(`resume.${key}`)}</span>
                  <span>
                    {min}〜{max}{t("chars")}
                  </span>
                </div>
                <div className="h-2 bg-gray-100 rounded-full overflow-hidden">
                  <div
                    className={`h-full rounded-full transition-all duration-300 ${
                      inRange
                        ? "bg-green-400"
                        : over
                        ? "bg-red-400"
                        : "bg-primary/60"
                    }`}
                    style={{ width: `${pct}%` }}
                  />
                </div>
              </div>
            );
          })}
        </div>
        <p className="text-xs text-gray-400 mt-3">{t("resumeNote")}</p>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 日本語翻訳ファイル作成**

`src/messages/ja/tools/character-counter-jp.json`:

```json
{
  "title": "原稿用紙換算・文字数カウンター",
  "description": "日本語テキストの文字数を400字詰め原稿用紙換算・X(Twitter)文字数・履歴書目安で確認できる無料オンラインツール。",
  "keywords": [
    "原稿用紙 換算 文字数",
    "400字詰め 枚数 計算",
    "ツイート 文字数 カウント",
    "履歴書 文字数 目安",
    "全角 半角 文字数 カウント",
    "X Twitter 文字数 確認"
  ],
  "placeholder": "ここにテキストを入力またはペーストしてください...",
  "stats": {
    "total": "総文字数",
    "withoutNewlines": "改行除く",
    "halfWidth": "半角文字",
    "fullWidth": "全角文字"
  },
  "sections": {
    "genkou": "📄 原稿用紙換算（400字詰め）",
    "twitter": "🐦 X / Twitter 文字数",
    "resume": "📋 履歴書・ES 文字数目安"
  },
  "genkouUnit": "枚",
  "genkouNote": "400字詰め原稿用紙での換算枚数です。",
  "over": "字オーバー",
  "remaining": "字残り",
  "twitterNote": "全角・CJK文字は2文字分、半角英数は1文字分としてカウントします（X/Twitter の重み付けカウント）。",
  "resume": {
    "motivation": "志望動機",
    "selfPr": "自己PR",
    "activities": "学生時代に力を入れたこと（ガクチカ）"
  },
  "chars": "字",
  "resumeNote": "各欄の文字数目安です。改行は含みません。あくまで参考値としてご利用ください。",
  "howToUse": [
    {
      "label": "テキストを入力する",
      "description": "テキストエリアに文章を入力またはペーストします。入力と同時にリアルタイムで各カウントが更新されます。"
    },
    {
      "label": "原稿用紙・Twitter文字数を確認する",
      "description": "400字詰め原稿用紙の換算枚数と、X/Twitter の重み付き文字数（全角=2・半角=1）がプログレスバーとともに表示されます。"
    },
    {
      "label": "履歴書・ESの文字数目安と比較する",
      "description": "志望動機・自己PR・ガクチカの推奨文字数帯をカラーバーで確認できます。緑＝適切・赤＝超過です。"
    }
  ],
  "faq": [
    {
      "question": "400字詰め原稿用紙で何枚か計算したい",
      "answer": "総文字数 ÷ 400 で換算枚数を計算しています。改行も1文字としてカウントしています。例えば1200字のテキストは3.0枚になります。"
    },
    {
      "question": "TwitterやXの文字数制限を確認したい",
      "answer": "X/Twitterでは全角・CJK文字（日本語・中国語・韓国語など）が2ウェイトとしてカウントされます。上限の280ウェイトは日本語140字に相当します。このツールはその重み付きカウントを表示します。"
    },
    {
      "question": "半角文字と全角文字の違いは？",
      "answer": "半角文字はASCII（英数字・記号）と半角カタカナ、全角文字はひらがな・カタカナ・漢字・全角英数などです。一般に全角文字は半角文字の2倍の幅で表示されます。"
    },
    {
      "question": "履歴書の志望動機は何文字が適切ですか？",
      "answer": "一般的な就職活動では志望動機・自己PRは100〜300字程度が目安です。ただし企業によって異なるため、募集要項で指定された文字数を確認してください。このツールの目安はあくまで参考値です。"
    },
    {
      "question": "通常の文字数カウンターとの違いは？",
      "answer": "通常の文字数カウンターは総文字数のみを表示します。このツールは原稿用紙換算・Twitter文字数・履歴書目安という日本語ならではのユースケースに特化しています。"
    }
  ]
}
```

- [ ] **Step 3: 英語翻訳ファイル作成**

`src/messages/en/tools/character-counter-jp.json`:

```json
{
  "title": "Japanese Character Counter & Manuscript Converter",
  "description": "Count Japanese text characters with genkou yoshi (400-char manuscript paper) conversion, X/Twitter weighted count, and resume length guide. Free online tool.",
  "keywords": [
    "japanese character counter",
    "genkou yoshi converter",
    "manuscript paper calculator",
    "twitter character count japanese",
    "full-width half-width counter",
    "resume character limit"
  ],
  "placeholder": "Type or paste your text here...",
  "stats": {
    "total": "Total chars",
    "withoutNewlines": "No newlines",
    "halfWidth": "Half-width",
    "fullWidth": "Full-width"
  },
  "sections": {
    "genkou": "📄 Genkou Yoshi (400 chars/page)",
    "twitter": "🐦 X / Twitter Character Count",
    "resume": "📋 Resume / Cover Letter Guide"
  },
  "genkouUnit": "pages",
  "genkouNote": "Equivalent number of 400-character manuscript paper pages.",
  "over": "over the limit",
  "remaining": "remaining",
  "twitterNote": "Full-width/CJK characters count as 2, half-width characters count as 1 (X/Twitter weighted count).",
  "resume": {
    "motivation": "Motivation letter",
    "selfPr": "Self-PR",
    "activities": "Extra-curricular activities"
  },
  "chars": " chars",
  "resumeNote": "Approximate character count guidelines. Newlines are excluded. For reference only.",
  "howToUse": [
    {
      "label": "Enter your text",
      "description": "Type or paste text into the textarea. All counts update in real time."
    },
    {
      "label": "Check manuscript pages and Twitter count",
      "description": "See the equivalent number of 400-char manuscript pages and the X/Twitter weighted character count (full-width=2, half-width=1) with progress bars."
    },
    {
      "label": "Compare with resume guidelines",
      "description": "Recommended character ranges for common resume sections are shown as color bars. Green = within range, red = over the limit."
    }
  ],
  "faq": [
    {
      "question": "How is the manuscript page count calculated?",
      "answer": "The total character count (including newlines) is divided by 400. For example, 1200 characters = 3.0 pages of 400-character manuscript paper."
    },
    {
      "question": "How does the Twitter/X character count work?",
      "answer": "X/Twitter counts CJK (Japanese, Chinese, Korean) and full-width characters as 2 units each. The 280-unit limit is equivalent to 140 full-width Japanese characters."
    },
    {
      "question": "What is the difference between half-width and full-width characters?",
      "answer": "Half-width characters include ASCII (letters, numbers, symbols) and half-width katakana. Full-width characters include hiragana, katakana, kanji, and full-width alphanumerics."
    },
    {
      "question": "How many characters should a cover letter be?",
      "answer": "In Japanese job applications, motivation letters and self-PR sections are typically 100–300 characters. Check the job posting for specific requirements. The guidelines shown are for reference only."
    },
    {
      "question": "How is this different from the regular character counter?",
      "answer": "The regular character counter shows total character count. This tool specializes in Japan-specific use cases: manuscript paper conversion, Twitter weighted count, and resume guidelines."
    }
  ]
}
```

- [ ] **Step 4: toolsRegistry.ts に追加** (`text-deduplicator` の後)

```ts
  {
    slug: "character-counter-jp",
    category: "text",
    icon: "📃",
    component: "CharacterCounterJp",
    updatedAt: "2026-04-18",
  },
```

- [ ] **Step 5: toolComponents.tsx に追加** (`TextDeduplicator` の後)

```tsx
  CharacterCounterJp: dynamic(() => import("@/tools/CharacterCounterJp"), { loading: Loading }),
```

- [ ] **Step 6: ビルド確認**

```bash
npm run build
```

- [ ] **Step 7: コミット**

```bash
git add src/tools/CharacterCounterJp.tsx \
        src/messages/ja/tools/character-counter-jp.json \
        src/messages/en/tools/character-counter-jp.json \
        src/lib/toolsRegistry.ts \
        src/lib/toolComponents.tsx
git commit -m "[ツール追加] character-counter-jp: 原稿用紙換算・文字数カウンター"
```

---

### Task 3: number-formatter — 数値フォーマッター

**Files:**
- Create: `src/tools/NumberFormatter.tsx`
- Create: `src/messages/ja/tools/number-formatter.json`
- Create: `src/messages/en/tools/number-formatter.json`
- Modify: `src/lib/toolsRegistry.ts`
- Modify: `src/lib/toolComponents.tsx`

- [ ] **Step 1: コンポーネント作成**

`src/tools/NumberFormatter.tsx`:

```tsx
"use client";

import { useState, useMemo } from "react";
import { useTranslations } from "next-intl";

function toJapaneseUnit(n: number, dec: number): string {
  const abs = Math.abs(n);
  const sign = n < 0 ? "-" : "";
  if (abs >= 1_0000_0000_0000) {
    return `${sign}${(abs / 1_0000_0000_0000).toFixed(dec)}兆`;
  }
  if (abs >= 1_0000_0000) {
    return `${sign}${(abs / 1_0000_0000).toFixed(dec)}億`;
  }
  if (abs >= 1_0000) {
    return `${sign}${(abs / 1_0000).toFixed(dec)}万`;
  }
  return `${sign}${abs.toFixed(dec)}`;
}

type FormatItem = { key: string; label: string; value: string };

export default function NumberFormatter() {
  const t = useTranslations("number-formatter");
  const [input, setInput] = useState("");
  const [decimals, setDecimals] = useState(0);
  const [copied, setCopied] = useState<string | null>(null);

  const raw = input.replace(/,/g, "").replace(/\s/g, "");
  const n = parseFloat(raw);
  const isValid = raw !== "" && Number.isFinite(n);

  const formats: FormatItem[] = useMemo(() => {
    if (!isValid) return [];
    const opt = { minimumFractionDigits: decimals, maximumFractionDigits: decimals };
    return [
      {
        key: "us",
        label: t("formats.us"),
        value: new Intl.NumberFormat("en-US", opt).format(n),
      },
      {
        key: "eu",
        label: t("formats.eu"),
        value: new Intl.NumberFormat("de-DE", opt).format(n),
      },
      {
        key: "space",
        label: t("formats.space"),
        value: new Intl.NumberFormat("fr-FR", opt).format(n),
      },
      {
        key: "jpy",
        label: t("formats.jpy"),
        value: new Intl.NumberFormat("ja-JP", {
          style: "currency",
          currency: "JPY",
        }).format(n),
      },
      {
        key: "usd",
        label: t("formats.usd"),
        value: new Intl.NumberFormat("en-US", {
          style: "currency",
          currency: "USD",
          minimumFractionDigits: 2,
          maximumFractionDigits: 2,
        }).format(n),
      },
      {
        key: "eur",
        label: t("formats.eur"),
        value: new Intl.NumberFormat("de-DE", {
          style: "currency",
          currency: "EUR",
          minimumFractionDigits: 2,
          maximumFractionDigits: 2,
        }).format(n),
      },
      {
        key: "jpUnit",
        label: t("formats.jpUnit"),
        value: toJapaneseUnit(n, decimals),
      },
    ];
  }, [n, decimals, isValid, t]);

  async function handleCopy(value: string, key: string) {
    await navigator.clipboard.writeText(value);
    setCopied(key);
    setTimeout(() => setCopied(null), 2000);
  }

  return (
    <div className="space-y-4">
      <div className="flex flex-col sm:flex-row gap-3">
        <div className="flex-1">
          <label className="block text-sm font-medium text-gray-700 mb-1.5">
            {t("inputLabel")}
          </label>
          <input
            type="text"
            inputMode="decimal"
            value={input}
            onChange={(e) => setInput(e.target.value)}
            placeholder={t("placeholder")}
            className="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm
                       focus:outline-none focus:ring-2 focus:ring-primary/30 focus:border-primary"
          />
        </div>
        <div>
          <label className="block text-sm font-medium text-gray-700 mb-1.5">
            {t("decimals")}
          </label>
          <select
            value={decimals}
            onChange={(e) => setDecimals(Number(e.target.value))}
            className="px-3 py-2 border border-gray-300 rounded-lg text-sm
                       focus:outline-none focus:ring-2 focus:ring-primary/30 focus:border-primary"
          >
            {[0, 1, 2, 3, 4].map((d) => (
              <option key={d} value={d}>
                {d}
              </option>
            ))}
          </select>
        </div>
      </div>

      {raw && !isValid && (
        <p className="text-sm text-red-500">{t("invalid")}</p>
      )}

      {isValid && (
        <div className="divide-y divide-gray-100 rounded-xl border border-gray-200 overflow-hidden">
          {formats.map(({ key, label, value }) => (
            <div
              key={key}
              className="flex items-center justify-between px-4 py-3 hover:bg-gray-50 transition-colors"
            >
              <span className="text-sm text-gray-500 w-36 shrink-0">{label}</span>
              <span className="text-sm font-mono font-medium flex-1 text-center px-2">
                {value}
              </span>
              <button
                type="button"
                onClick={() => handleCopy(value, key)}
                className="text-xs px-3 py-1 rounded bg-gray-200 hover:bg-gray-300 text-gray-700 transition-colors shrink-0"
              >
                {copied === key ? t("copied") : t("copy")}
              </button>
            </div>
          ))}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 日本語翻訳ファイル作成**

`src/messages/ja/tools/number-formatter.json`:

```json
{
  "title": "数値フォーマッター",
  "description": "数値を桁区切り・通貨・万億兆単位など様々な形式に一括変換。US/EU形式から日本語の万・億・兆表記まで対応した無料オンラインツール。",
  "keywords": [
    "数値 フォーマット 変換",
    "桁区切り 変換 オンライン",
    "万 億 兆 変換",
    "通貨 フォーマット 無料",
    "数字 カンマ 区切り",
    "数値 表記 変換"
  ],
  "inputLabel": "変換する数値",
  "placeholder": "例: 1234567.89",
  "decimals": "小数点以下桁数",
  "invalid": "有効な数値を入力してください",
  "copy": "コピー",
  "copied": "コピー済み",
  "formats": {
    "us": "US（カンマ区切り）",
    "eu": "EU（ドット区切り）",
    "space": "スペース区切り",
    "jpy": "円（JPY）",
    "usd": "ドル（USD）",
    "eur": "ユーロ（EUR）",
    "jpUnit": "日本語単位（万・億・兆）"
  },
  "howToUse": [
    {
      "label": "数値を入力する",
      "description": "変換したい数値を入力します。カンマやスペースが含まれていても自動的に除去して処理します。"
    },
    {
      "label": "小数点以下桁数を選ぶ",
      "description": "0〜4桁の範囲で小数点以下の表示桁数を選択します。通貨表示（JPY/USD/EUR）は自動設定されます。"
    },
    {
      "label": "目的の形式をコピーする",
      "description": "7種類のフォーマットが一覧表示されます。使いたい形式の右にあるコピーボタンをクリックしてください。"
    }
  ],
  "faq": [
    {
      "question": "数字にカンマ（桁区切り）を入れたい",
      "answer": "入力すると「US（カンマ区切り）」形式で1,234,567のようにカンマ区切りが表示されます。コピーボタンでそのままコピーできます。"
    },
    {
      "question": "1億2345万などの日本語表記に変換したい",
      "answer": "「日本語単位（万・億・兆）」形式で、123456789 → 1億2345.7万 のように変換されます。小数点以下桁数の設定も反映されます。"
    },
    {
      "question": "ヨーロッパ式（ドット区切り、カンマが小数点）の表記にしたい",
      "answer": "「EU（ドット区切り）」形式を使うと 1.234.567,89 のようにEUで一般的な数値表記になります。"
    },
    {
      "question": "通貨記号付きの表示はできますか？",
      "answer": "はい。円（¥）・ドル（$）・ユーロ（€）の3種類に対応しています。Intl.NumberFormat APIを使い、各通貨の慣習的な表記（位置・記号）を自動適用します。"
    },
    {
      "question": "入力した数値はどう処理されますか？",
      "answer": "処理はすべてブラウザ上で完結します。カンマやスペースを除去した後、JavaScriptのIntl.NumberFormatで変換します。サーバーへの送信は一切行いません。"
    }
  ]
}
```

- [ ] **Step 3: 英語翻訳ファイル作成**

`src/messages/en/tools/number-formatter.json`:

```json
{
  "title": "Number Formatter",
  "description": "Format numbers in US, EU, Japanese unit (万/億/兆), and currency formats all at once. Free online number formatting tool.",
  "keywords": [
    "number formatter online",
    "thousand separator converter",
    "number format converter free",
    "currency format tool",
    "japanese number units",
    "comma separated number"
  ],
  "inputLabel": "Number to format",
  "placeholder": "e.g. 1234567.89",
  "decimals": "Decimal places",
  "invalid": "Please enter a valid number",
  "copy": "Copy",
  "copied": "Copied!",
  "formats": {
    "us": "US (comma separated)",
    "eu": "EU (dot separated)",
    "space": "Space separated",
    "jpy": "Yen (JPY)",
    "usd": "Dollar (USD)",
    "eur": "Euro (EUR)",
    "jpUnit": "Japanese units (万/億/兆)"
  },
  "howToUse": [
    {
      "label": "Enter a number",
      "description": "Type the number you want to format. Commas and spaces in the input are automatically stripped."
    },
    {
      "label": "Choose decimal places",
      "description": "Select 0–4 decimal places. Currency formats (JPY/USD/EUR) use their standard decimal settings."
    },
    {
      "label": "Copy the format you need",
      "description": "Seven formats are displayed at once. Click the Copy button next to the format you want."
    }
  ],
  "faq": [
    {
      "question": "How do I add comma separators to a number?",
      "answer": "Enter your number and the 'US (comma separated)' row shows it as 1,234,567. Click Copy to grab it instantly."
    },
    {
      "question": "What are the Japanese unit formats (万/億/兆)?",
      "answer": "Japanese text commonly expresses large numbers in units of 万 (10,000), 億 (100,000,000), and 兆 (1,000,000,000,000). For example 123456789 becomes 1億2345.7万."
    },
    {
      "question": "What is the EU number format?",
      "answer": "In many European countries, periods (.) are used as thousands separators and commas (,) as decimal separators. For example: 1.234.567,89."
    },
    {
      "question": "Can I format numbers as currency?",
      "answer": "Yes. Japanese Yen (¥), US Dollar ($), and Euro (€) are supported with the correct symbol placement and standard decimal handling via the Intl.NumberFormat API."
    },
    {
      "question": "Is my data sent to a server?",
      "answer": "No. All formatting is done in your browser using the built-in Intl.NumberFormat API. No data is sent anywhere."
    }
  ]
}
```

- [ ] **Step 4: toolsRegistry.ts に追加** (`character-counter-jp` の後)

```ts
  {
    slug: "number-formatter",
    category: "convert",
    icon: "🔢",
    component: "NumberFormatter",
    updatedAt: "2026-04-18",
  },
```

- [ ] **Step 5: toolComponents.tsx に追加** (`CharacterCounterJp` の後)

```tsx
  NumberFormatter: dynamic(() => import("@/tools/NumberFormatter"), { loading: Loading }),
```

- [ ] **Step 6: ビルド確認**

```bash
npm run build
```

- [ ] **Step 7: コミット**

```bash
git add src/tools/NumberFormatter.tsx \
        src/messages/ja/tools/number-formatter.json \
        src/messages/en/tools/number-formatter.json \
        src/lib/toolsRegistry.ts \
        src/lib/toolComponents.tsx
git commit -m "[ツール追加] number-formatter: 数値フォーマッター"
```

---

### Task 4: char-frequency — 文字頻度分析

**Files:**
- Create: `src/tools/CharFrequency.tsx`
- Create: `src/messages/ja/tools/char-frequency.json`
- Create: `src/messages/en/tools/char-frequency.json`
- Modify: `src/lib/toolsRegistry.ts`
- Modify: `src/lib/toolComponents.tsx`

- [ ] **Step 1: コンポーネント作成**

`src/tools/CharFrequency.tsx`:

```tsx
"use client";

import { useState, useMemo } from "react";
import { useTranslations } from "next-intl";

type Tab = "char" | "word";
type Entry = { key: string; count: number };

function getCharFreq(text: string, excludeSpaces: boolean, excludePunct: boolean): Entry[] {
  const map = new Map<string, number>();
  for (const ch of text) {
    if (excludeSpaces && /\s/.test(ch)) continue;
    if (excludePunct && /[^\p{L}\p{N}]/u.test(ch)) continue;
    map.set(ch, (map.get(ch) ?? 0) + 1);
  }
  return [...map.entries()]
    .map(([key, count]) => ({ key, count }))
    .sort((a, b) => b.count - a.count);
}

function getWordFreq(text: string): Entry[] {
  const words = text.toLowerCase().match(/\p{L}+/gu) ?? [];
  const map = new Map<string, number>();
  for (const w of words) map.set(w, (map.get(w) ?? 0) + 1);
  return [...map.entries()]
    .map(([key, count]) => ({ key, count }))
    .sort((a, b) => b.count - a.count);
}

export default function CharFrequency() {
  const t = useTranslations("char-frequency");
  const [input, setInput] = useState("");
  const [tab, setTab] = useState<Tab>("char");
  const [excludeSpaces, setExcludeSpaces] = useState(true);
  const [excludePunct, setExcludePunct] = useState(false);
  const [showAll, setShowAll] = useState(false);

  const { entries, totalChars, uniqueChars, uniqueWords } = useMemo(() => {
    const charEntries = getCharFreq(input, excludeSpaces, excludePunct);
    const wordEntries = getWordFreq(input);
    return {
      entries: tab === "char" ? charEntries : wordEntries,
      totalChars: [...input].length,
      uniqueChars: new Set([...input].filter((c) => !/\s/.test(c))).size,
      uniqueWords: new Set((input.toLowerCase().match(/\p{L}+/gu) ?? [])).size,
    };
  }, [input, tab, excludeSpaces, excludePunct]);

  const TOP = 20;
  const displayed = showAll ? entries : entries.slice(0, TOP);
  const max = entries[0]?.count ?? 1;

  return (
    <div className="space-y-4">
      <div>
        <textarea
          value={input}
          onChange={(e) => setInput(e.target.value)}
          placeholder={t("placeholder")}
          rows={6}
          className="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm font-mono resize-y
                     focus:outline-none focus:ring-2 focus:ring-primary/30 focus:border-primary"
        />
      </div>

      {/* 統計バッジ */}
      {input && (
        <div className="flex flex-wrap gap-2 text-xs">
          <span className="px-3 py-1 rounded-full bg-blue-50 text-blue-700">
            {t("stats.totalChars")} {totalChars.toLocaleString()}
          </span>
          <span className="px-3 py-1 rounded-full bg-purple-50 text-purple-700">
            {t("stats.uniqueChars")} {uniqueChars.toLocaleString()}
          </span>
          <span className="px-3 py-1 rounded-full bg-green-50 text-green-700">
            {t("stats.uniqueWords")} {uniqueWords.toLocaleString()}
          </span>
        </div>
      )}

      {/* タブ + オプション */}
      <div className="flex flex-wrap items-center justify-between gap-3">
        <div className="flex gap-2">
          {(["char", "word"] as Tab[]).map((tb) => (
            <button
              key={tb}
              type="button"
              onClick={() => { setTab(tb); setShowAll(false); }}
              className={`px-4 py-1.5 rounded-lg text-sm font-medium transition-colors ${
                tab === tb
                  ? "bg-primary text-white"
                  : "bg-gray-100 text-gray-700 hover:bg-gray-200"
              }`}
            >
              {t(`tabs.${tb}`)}
            </button>
          ))}
        </div>
        {tab === "char" && (
          <div className="flex flex-wrap gap-4 text-sm">
            {(
              [
                { val: excludeSpaces, set: setExcludeSpaces, key: "excludeSpaces" },
                { val: excludePunct, set: setExcludePunct, key: "excludePunct" },
              ] as const
            ).map(({ val, set, key }) => (
              <label key={key} className="flex items-center gap-2 cursor-pointer select-none">
                <input
                  type="checkbox"
                  checked={val}
                  onChange={(e) => set(e.target.checked)}
                  className="w-4 h-4 accent-primary"
                />
                <span className="text-gray-700">{t(`options.${key}`)}</span>
              </label>
            ))}
          </div>
        )}
      </div>

      {/* 棒グラフ */}
      {entries.length > 0 ? (
        <div className="space-y-1.5">
          <div className="grid grid-cols-[24px_60px_1fr_40px] gap-x-3 text-xs text-gray-400 mb-2 px-1">
            <span>#</span>
            <span>{t("columnKey")}</span>
            <span></span>
            <span className="text-right">{t("columnCount")}</span>
          </div>
          {displayed.map(({ key, count }, i) => (
            <div key={`${key}-${i}`} className="grid grid-cols-[24px_60px_1fr_40px] items-center gap-x-3 text-sm">
              <span className="text-xs text-gray-400 text-right">{i + 1}</span>
              <span className="font-mono text-gray-700 truncate">
                {key === " " ? t("space") : key}
              </span>
              <div className="h-4 bg-gray-100 rounded-full overflow-hidden">
                <div
                  className="h-full bg-primary/60 rounded-full"
                  style={{ width: `${(count / max) * 100}%` }}
                />
              </div>
              <span className="text-xs text-gray-500 text-right tabular-nums">{count}</span>
            </div>
          ))}
          {entries.length > TOP && (
            <button
              type="button"
              onClick={() => setShowAll((v) => !v)}
              className="w-full text-xs text-primary hover:underline mt-2 py-1"
            >
              {showAll
                ? t("showLess")
                : t("showAll", { count: entries.length - TOP })}
            </button>
          )}
        </div>
      ) : (
        input && (
          <p className="text-sm text-gray-400 text-center py-4">{t("noData")}</p>
        )
      )}
    </div>
  );
}
```

注意: `t("showAll", { count: N })` のICU補間は next-intl 4.x でサポート済み。翻訳値は `"残り{count}件を表示"` 形式で記述する。

- [ ] **Step 2: 日本語翻訳ファイル作成**

`src/messages/ja/tools/char-frequency.json`:

```json
{
  "title": "文字頻度分析ツール",
  "description": "テキストの文字・単語の出現頻度を集計しランキング表示。CSSバーグラフで視覚化。日本語・英語テキスト両対応の無料オンラインツール。",
  "keywords": [
    "文字 頻度 分析",
    "単語 出現回数 カウント",
    "テキスト 頻度 ランキング",
    "文字 出現頻度 無料",
    "ワード 頻度 集計"
  ],
  "placeholder": "分析するテキストをここに入力またはペーストしてください...",
  "tabs": {
    "char": "文字頻度",
    "word": "単語頻度"
  },
  "stats": {
    "totalChars": "総文字数:",
    "uniqueChars": "ユニーク文字:",
    "uniqueWords": "ユニーク単語:"
  },
  "options": {
    "excludeSpaces": "空白を除く",
    "excludePunct": "記号・句読点を除く"
  },
  "columnKey": "文字 / 単語",
  "columnCount": "回数",
  "space": "（スペース）",
  "showAll": "さらに{count}件を表示",
  "showLess": "折りたたむ",
  "noData": "該当するデータがありません（フィルター設定を確認してください）",
  "howToUse": [
    {
      "label": "テキストを入力する",
      "description": "分析したいテキストを入力またはペーストします。入力後すぐに頻度が集計・表示されます。"
    },
    {
      "label": "文字頻度 / 単語頻度を切り替える",
      "description": "「文字頻度」では各文字の出現回数を、「単語頻度」では各単語（英語はスペース区切り）の出現回数をランキング表示します。"
    },
    {
      "label": "フィルターとランキングを確認する",
      "description": "空白・記号を除くオプションで不要な文字を除外できます。Top20が表示され、「さらに表示」ボタンで全件確認できます。"
    }
  ],
  "faq": [
    {
      "question": "文章中で最も多く使われている文字・単語を知りたい",
      "answer": "テキストを入力すると出現回数の多い順にTop20のランキングが棒グラフで表示されます。「さらに表示」で全件確認できます。"
    },
    {
      "question": "日本語テキストの単語頻度は正確ですか？",
      "answer": "単語頻度はUnicode文字クラス（\\p{L}）を使って単語境界を検出します。英語はスペース区切りで正確に分割できますが、日本語は形態素解析なしの簡易分割のため、精度は参考程度とお考えください。"
    },
    {
      "question": "スペースや記号をランキングから除外したい",
      "answer": "「空白を除く」または「記号・句読点を除く」チェックボックスをオンにすると、対象外になります。両方オンにすることも可能です。"
    },
    {
      "question": "どんな用途に使えますか？",
      "answer": "英語ライティングで特定の単語を使いすぎていないか確認する・日本語テキストで頻出漢字を把握する・文章の特徴を分析するなどの用途に使えます。"
    },
    {
      "question": "入力テキストはサーバーに送信されますか？",
      "answer": "いいえ。すべての集計処理はブラウザ上で完結します。テキストが外部に送信されることは一切ありません。"
    }
  ]
}
```

- [ ] **Step 3: 英語翻訳ファイル作成**

`src/messages/en/tools/char-frequency.json`:

```json
{
  "title": "Character & Word Frequency Analyzer",
  "description": "Analyze character and word frequency in text with ranked bar charts. Switch between character and word frequency. Free online tool.",
  "keywords": [
    "character frequency analyzer",
    "word frequency counter online",
    "text frequency analysis",
    "letter frequency tool free",
    "word count ranking"
  ],
  "placeholder": "Type or paste text to analyze...",
  "tabs": {
    "char": "Character Frequency",
    "word": "Word Frequency"
  },
  "stats": {
    "totalChars": "Total chars:",
    "uniqueChars": "Unique chars:",
    "uniqueWords": "Unique words:"
  },
  "options": {
    "excludeSpaces": "Exclude spaces",
    "excludePunct": "Exclude punctuation"
  },
  "columnKey": "Char / Word",
  "columnCount": "Count",
  "space": "(space)",
  "showAll": "Show {count} more",
  "showLess": "Show less",
  "noData": "No data found. Try adjusting the filter options.",
  "howToUse": [
    {
      "label": "Enter your text",
      "description": "Type or paste the text you want to analyze. The frequency ranking updates instantly."
    },
    {
      "label": "Switch between character and word frequency",
      "description": "Character frequency shows how often each character appears. Word frequency counts occurrences of each word."
    },
    {
      "label": "Filter and explore the ranking",
      "description": "Use the options to exclude spaces or punctuation. Top 20 are shown by default; click 'Show more' to see all."
    }
  ],
  "faq": [
    {
      "question": "How do I find the most common words in my text?",
      "answer": "Switch to the 'Word Frequency' tab. Words are counted in a case-insensitive way and ranked from most to least frequent."
    },
    {
      "question": "How accurate is word frequency for Japanese text?",
      "answer": "Word splitting uses Unicode character classes (\\p{L}) without morphological analysis. English splits accurately on spaces; Japanese results are approximate since word boundaries are ambiguous without a tokenizer."
    },
    {
      "question": "How do I exclude spaces and punctuation from the results?",
      "answer": "Check 'Exclude spaces' to remove whitespace characters, and 'Exclude punctuation' to remove symbols and punctuation marks. Both can be enabled simultaneously."
    },
    {
      "question": "What can I use this tool for?",
      "answer": "Common uses: checking if you overuse certain words in English writing, finding frequent kanji in Japanese text, and analyzing text patterns for linguistic research."
    },
    {
      "question": "Is my text sent to a server?",
      "answer": "No. All analysis runs entirely in your browser. Your text is never sent to any external server."
    }
  ]
}
```

- [ ] **Step 4: toolsRegistry.ts に追加** (`number-formatter` の後)

```ts
  {
    slug: "char-frequency",
    category: "text",
    icon: "📊",
    component: "CharFrequency",
    updatedAt: "2026-04-18",
  },
```

- [ ] **Step 5: toolComponents.tsx に追加** (`NumberFormatter` の後)

```tsx
  CharFrequency: dynamic(() => import("@/tools/CharFrequency"), { loading: Loading }),
```

- [ ] **Step 6: ビルド確認**

```bash
npm run build
```

- [ ] **Step 7: コミット**

```bash
git add src/tools/CharFrequency.tsx \
        src/messages/ja/tools/char-frequency.json \
        src/messages/en/tools/char-frequency.json \
        src/lib/toolsRegistry.ts \
        src/lib/toolComponents.tsx
git commit -m "[ツール追加] char-frequency: 文字頻度分析ツール"
```

---

### Task 5: countdown-timer — カウントダウンタイマー

**Files:**
- Create: `src/tools/CountdownTimer.tsx`
- Create: `src/messages/ja/tools/countdown-timer.json`
- Create: `src/messages/en/tools/countdown-timer.json`
- Modify: `src/lib/toolsRegistry.ts`
- Modify: `src/lib/toolComponents.tsx`

- [ ] **Step 1: コンポーネント作成**

`src/tools/CountdownTimer.tsx`:

```tsx
"use client";

import { useState, useEffect } from "react";
import { useTranslations } from "next-intl";

type CEvent = {
  id: string;
  name: string;
  target: string; // ISO 8601 datetime string
};

type TimeLeft = {
  elapsed: boolean;
  d: number;
  h: number;
  m: number;
  s: number;
  totalMs: number;
};

function calcTimeLeft(target: string): TimeLeft {
  const diff = new Date(target).getTime() - Date.now();
  const abs = Math.abs(diff);
  const totalSec = Math.floor(abs / 1000);
  return {
    elapsed: diff < 0,
    totalMs: abs,
    d: Math.floor(totalSec / 86400),
    h: Math.floor((totalSec % 86400) / 3600),
    m: Math.floor((totalSec % 3600) / 60),
    s: totalSec % 60,
  };
}

const STORAGE_KEY = "quicker:countdowns";
const MAX_EVENTS = 10;

export default function CountdownTimer() {
  const t = useTranslations("countdown-timer");
  const [events, setEvents] = useState<CEvent[]>([]);
  const [name, setName] = useState("");
  const [target, setTarget] = useState("");
  const [tick, setTick] = useState(0);

  useEffect(() => {
    try {
      const stored = localStorage.getItem(STORAGE_KEY);
      if (stored) setEvents(JSON.parse(stored) as CEvent[]);
    } catch {}
  }, []);

  useEffect(() => {
    const id = setInterval(() => setTick((n) => n + 1), 1000);
    return () => clearInterval(id);
  }, []);

  // tick は再計算トリガーのみ — ESLint exhaustive-deps の警告を避けるため変数を参照
  void tick;

  function persist(next: CEvent[]) {
    setEvents(next);
    localStorage.setItem(STORAGE_KEY, JSON.stringify(next));
  }

  function handleAdd() {
    if (!name.trim() || !target || events.length >= MAX_EVENTS) return;
    persist([
      ...events,
      { id: crypto.randomUUID(), name: name.trim(), target },
    ]);
    setName("");
    setTarget("");
  }

  function handleDelete(id: string) {
    persist(events.filter((e) => e.id !== id));
  }

  return (
    <div className="space-y-6">
      {/* 追加フォーム */}
      <div className="tool-card p-4">
        <h2 className="text-sm font-semibold text-gray-700 mb-3">{t("addEvent")}</h2>
        <div className="flex flex-col sm:flex-row gap-3">
          <input
            type="text"
            value={name}
            onChange={(e) => setName(e.target.value)}
            onKeyDown={(e) => e.key === "Enter" && handleAdd()}
            placeholder={t("namePlaceholder")}
            maxLength={50}
            className="flex-1 px-3 py-2 border border-gray-300 rounded-lg text-sm
                       focus:outline-none focus:ring-2 focus:ring-primary/30 focus:border-primary"
          />
          <input
            type="datetime-local"
            value={target}
            onChange={(e) => setTarget(e.target.value)}
            className="px-3 py-2 border border-gray-300 rounded-lg text-sm
                       focus:outline-none focus:ring-2 focus:ring-primary/30 focus:border-primary"
          />
          <button
            type="button"
            onClick={handleAdd}
            disabled={!name.trim() || !target || events.length >= MAX_EVENTS}
            className="px-5 py-2 bg-primary text-white rounded-lg text-sm font-medium
                       hover:bg-primary/90 disabled:opacity-40 transition-colors"
          >
            {t("add")}
          </button>
        </div>
        {events.length >= MAX_EVENTS && (
          <p className="text-xs text-gray-400 mt-2">{t("maxReached")}</p>
        )}
      </div>

      {/* イベント一覧 */}
      {events.length === 0 ? (
        <p className="text-center text-sm text-gray-400 py-8">{t("noEvents")}</p>
      ) : (
        <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
          {events.map((ev) => {
            const tl = calcTimeLeft(ev.target);
            const urgent = !tl.elapsed && tl.totalMs < 7 * 24 * 3600 * 1000;
            return (
              <div
                key={ev.id}
                className={`tool-card p-4 relative ${urgent ? "border-l-4 border-l-accent" : ""}`}
              >
                <button
                  type="button"
                  onClick={() => handleDelete(ev.id)}
                  className="absolute top-3 right-3 w-6 h-6 flex items-center justify-center
                             text-gray-400 hover:text-gray-600 rounded text-xs"
                  aria-label={t("delete")}
                >
                  ✕
                </button>
                <p className="font-semibold text-sm truncate pr-8">{ev.name}</p>
                <p className="text-xs text-gray-400 mt-0.5">
                  {new Date(ev.target).toLocaleString()}
                </p>

                {tl.elapsed ? (
                  <p className="mt-3 text-sm text-gray-500">
                    {t("elapsed", { days: tl.d })}
                  </p>
                ) : (
                  <div className="mt-3 flex gap-4">
                    {(
                      [
                        { val: tl.d, key: "days" },
                        { val: tl.h, key: "hours" },
                        { val: tl.m, key: "minutes" },
                        { val: tl.s, key: "seconds" },
                      ] as const
                    ).map(({ val, key }) => (
                      <div key={key} className="text-center">
                        <div
                          className={`text-2xl font-bold tabular-nums ${
                            urgent ? "text-accent" : "text-primary"
                          }`}
                        >
                          {String(val).padStart(2, "0")}
                        </div>
                        <div className="text-xs text-gray-400">{t(key)}</div>
                      </div>
                    ))}
                  </div>
                )}
              </div>
            );
          })}
        </div>
      )}
    </div>
  );
}
```

注意: `t("elapsed", { days: tl.d })` のICU補間を使用。翻訳値: `"{days}日経過"` 形式で記述する。

- [ ] **Step 2: 日本語翻訳ファイル作成**

`src/messages/ja/tools/countdown-timer.json`:

```json
{
  "title": "カウントダウンタイマー",
  "description": "イベントまでの残り日時をリアルタイムカウントダウン。複数イベントをブラウザに保存できる無料オンラインタイマーツール。",
  "keywords": [
    "カウントダウン タイマー オンライン",
    "イベント 残り日数 カウント",
    "日付 カウントダウン 無料",
    "締め切り カウントダウン",
    "誕生日 カウントダウン"
  ],
  "addEvent": "イベントを追加",
  "namePlaceholder": "イベント名（例: 締め切り）",
  "add": "追加",
  "delete": "削除",
  "noEvents": "イベントがありません。上のフォームから追加してください。",
  "maxReached": "イベントは最大10件まで保存できます。",
  "elapsed": "{days}日経過",
  "days": "日",
  "hours": "時間",
  "minutes": "分",
  "seconds": "秒",
  "howToUse": [
    {
      "label": "イベントを追加する",
      "description": "イベント名と目標日時を入力して「追加」ボタンをクリックします。最大10件まで保存できます。"
    },
    {
      "label": "リアルタイムカウントダウンを確認する",
      "description": "残り日・時間・分・秒がリアルタイムで更新されます。7日以内のイベントはアクセントカラーでハイライトされます。"
    },
    {
      "label": "完了したイベントを削除する",
      "description": "カードの右上のボタンでイベントを削除できます。データはブラウザのlocalStorageに保存されており、ページを閉じても保持されます。"
    }
  ],
  "faq": [
    {
      "question": "イベントはページを閉じても保存されますか？",
      "answer": "はい。イベント情報はブラウザのlocalStorageに保存されます。ページを閉じてもデータは保持されますが、ブラウザのデータをクリアすると消えます。"
    },
    {
      "question": "何件までイベントを登録できますか？",
      "answer": "最大10件まで登録できます。10件に達するとフォームの「追加」ボタンが無効になります。不要なイベントを削除してから追加してください。"
    },
    {
      "question": "期日を過ぎたイベントはどうなりますか？",
      "answer": "カウントダウンがゼロになった後は「○日経過」という経過時間表示に切り替わります。自動的に削除されることはありません。"
    },
    {
      "question": "7日以内のイベントが色変わりするのはなぜですか？",
      "answer": "締め切りが近いことを視覚的に伝えるため、残り7日以内になるとカードの左ボーダーとカウント数字がアクセントカラー（赤系）に変わります。"
    },
    {
      "question": "別のデバイスと同期できますか？",
      "answer": "いいえ。データはブラウザのlocalStorageに保存されるため、同じブラウザ・同じデバイス上でのみ有効です。複数デバイスでの同期には対応していません。"
    }
  ]
}
```

- [ ] **Step 3: 英語翻訳ファイル作成**

`src/messages/en/tools/countdown-timer.json`:

```json
{
  "title": "Countdown Timer",
  "description": "Countdown to multiple events in real time. Save up to 10 events in your browser. Free online countdown timer tool.",
  "keywords": [
    "countdown timer online",
    "event countdown free",
    "days until date calculator",
    "deadline countdown",
    "birthday countdown timer"
  ],
  "addEvent": "Add Event",
  "namePlaceholder": "Event name (e.g. Project deadline)",
  "add": "Add",
  "delete": "Delete",
  "noEvents": "No events yet. Add one using the form above.",
  "maxReached": "Maximum of 10 events reached.",
  "elapsed": "{days} days elapsed",
  "days": "Days",
  "hours": "Hrs",
  "minutes": "Min",
  "seconds": "Sec",
  "howToUse": [
    {
      "label": "Add an event",
      "description": "Enter an event name and target date/time, then click Add. Up to 10 events can be saved."
    },
    {
      "label": "Watch the real-time countdown",
      "description": "Days, hours, minutes, and seconds update every second. Events within 7 days are highlighted in accent color."
    },
    {
      "label": "Delete completed events",
      "description": "Click the ✕ button on any card to remove it. Events are saved in your browser's localStorage and persist across page refreshes."
    }
  ],
  "faq": [
    {
      "question": "Are my events saved when I close the page?",
      "answer": "Yes. Events are saved to your browser's localStorage. They persist across page refreshes, but will be lost if you clear your browser data."
    },
    {
      "question": "How many events can I save?",
      "answer": "Up to 10 events. Once the limit is reached, the Add button is disabled. Delete an existing event to make room for a new one."
    },
    {
      "question": "What happens when the deadline passes?",
      "answer": "After the target time, the countdown switches to an elapsed time display (e.g. '3 days elapsed'). Events are not automatically removed."
    },
    {
      "question": "Why do events within 7 days change color?",
      "answer": "Events within 7 days are highlighted with an accent color on the card border and countdown numbers, giving a visual urgency indicator."
    },
    {
      "question": "Can I sync events across devices?",
      "answer": "No. Events are stored in your browser's localStorage, so they're only available on the same browser and device. Cross-device sync is not supported."
    }
  ]
}
```

- [ ] **Step 4: toolsRegistry.ts に追加** (`char-frequency` の後)

```ts
  {
    slug: "countdown-timer",
    category: "lifestyle",
    icon: "⏳",
    component: "CountdownTimer",
    updatedAt: "2026-04-18",
  },
```

- [ ] **Step 5: toolComponents.tsx に追加** (`CharFrequency` の後)

```tsx
  CountdownTimer: dynamic(() => import("@/tools/CountdownTimer"), { loading: Loading }),
```

- [ ] **Step 6: ビルド確認**

```bash
npm run build
```

- [ ] **Step 7: コミット**

```bash
git add src/tools/CountdownTimer.tsx \
        src/messages/ja/tools/countdown-timer.json \
        src/messages/en/tools/countdown-timer.json \
        src/lib/toolsRegistry.ts \
        src/lib/toolComponents.tsx
git commit -m "[ツール追加] countdown-timer: カウントダウンタイマー"
```

---

### Task 6: exif-viewer — 画像EXIFデータ閲覧

**Files:**
- Create: `src/tools/ExifViewer.tsx`
- Create: `src/messages/ja/tools/exif-viewer.json`
- Create: `src/messages/en/tools/exif-viewer.json`
- Modify: `src/lib/toolsRegistry.ts`
- Modify: `src/lib/toolComponents.tsx`

- [ ] **Step 1: コンポーネント作成（EXIFパーサー含む）**

`src/tools/ExifViewer.tsx`:

```tsx
"use client";

import { useState, useCallback } from "react";
import { useTranslations } from "next-intl";

// ── EXIF Parser ────────────────────────────────────────────────────────────

const IFD0_TAGS: Record<number, string> = {
  0x010f: "Make", 0x0110: "Model", 0x0131: "Software",
  0x0132: "DateTime", 0x013b: "Artist",
  0x011a: "XResolution", 0x011b: "YResolution", 0x0128: "ResolutionUnit",
  0x0112: "Orientation",
  0x8769: "__ExifIFD", 0x8825: "__GPSIFD",
};
const EXIF_TAGS: Record<number, string> = {
  0x9000: "ExifVersion", 0x9003: "DateTimeOriginal", 0x9004: "DateTimeDigitized",
  0xa002: "PixelXDimension", 0xa003: "PixelYDimension",
  0x829a: "ExposureTime", 0x829d: "FNumber", 0x8827: "ISOSpeedRatings",
  0x920a: "FocalLength", 0x9209: "Flash", 0x8822: "ExposureProgram",
  0x9201: "ShutterSpeedValue", 0x9202: "ApertureValue", 0x9204: "ExposureBiasValue",
  0xa001: "ColorSpace", 0xa217: "SensingMethod", 0x9207: "MeteringMode",
};
const GPS_TAGS: Record<number, string> = {
  0x0001: "GPSLatitudeRef", 0x0002: "GPSLatitude",
  0x0003: "GPSLongitudeRef", 0x0004: "GPSLongitude",
  0x0005: "GPSAltitudeRef", 0x0006: "GPSAltitude",
  0x001d: "GPSDateStamp",
};

type ExifResult = Record<string, string | number>;

const TYPE_SIZE = [0, 1, 1, 2, 4, 8, 1, 1, 2, 4, 8];

function readASCII(view: DataView, offset: number, count: number): string {
  let s = "";
  for (let i = 0; i < count - 1; i++) {
    const b = view.getUint8(offset + i);
    if (b === 0) break;
    s += String.fromCharCode(b);
  }
  return s.trim();
}

function readRational(view: DataView, offset: number, le: boolean, signed = false): number {
  const n = signed ? view.getInt32(offset, le) : view.getUint32(offset, le);
  const d = signed ? view.getInt32(offset + 4, le) : view.getUint32(offset + 4, le);
  return d === 0 ? 0 : n / d;
}

function readValue(
  view: DataView, type: number, count: number, valueOff: number, le: boolean
): string | number {
  switch (type) {
    case 1:  return view.getUint8(valueOff);
    case 2:  return readASCII(view, valueOff, count);
    case 3:  return view.getUint16(valueOff, le);
    case 4:  return view.getUint32(valueOff, le);
    case 5:  return readRational(view, valueOff, le);
    case 9:  return view.getInt32(valueOff, le);
    case 10: return readRational(view, valueOff, le, true);
    default: return `[${count}B]`;
  }
}

function readGPSCoord(view: DataView, valueOff: number, le: boolean): string {
  const deg = readRational(view, valueOff, le);
  const min = readRational(view, valueOff + 8, le);
  const sec = readRational(view, valueOff + 16, le);
  return `${deg}° ${min}' ${sec.toFixed(2)}"`;
}

function readIFD(
  view: DataView, ifdOffset: number, le: boolean,
  tags: Record<number, string>, out: ExifResult,
  isGPS = false
): { exifIFD?: number; gpsIFD?: number } {
  if (ifdOffset + 2 > view.byteLength) return {};
  const count = view.getUint16(ifdOffset, le);
  let exifIFD: number | undefined, gpsIFD: number | undefined;
  for (let i = 0; i < count; i++) {
    const base = ifdOffset + 2 + i * 12;
    if (base + 12 > view.byteLength) break;
    const tag = view.getUint16(base, le);
    const type = view.getUint16(base + 2, le);
    const cnt = view.getUint32(base + 4, le);
    const typeSize = type < TYPE_SIZE.length ? TYPE_SIZE[type] : 1;
    const valSize = typeSize * cnt;
    const valOff = valSize <= 4 ? base + 8 : view.getUint32(base + 8, le);
    if (tags[tag] === "__ExifIFD") { exifIFD = view.getUint32(base + 8, le); continue; }
    if (tags[tag] === "__GPSIFD")  { gpsIFD  = view.getUint32(base + 8, le); continue; }
    if (!tags[tag]) continue;
    try {
      if (isGPS && type === 5 && cnt === 3) {
        out[tags[tag]] = readGPSCoord(view, valOff, le);
      } else {
        out[tags[tag]] = readValue(view, type, cnt, valOff, le);
      }
    } catch {}
  }
  return { exifIFD, gpsIFD };
}

function parseExif(buffer: ArrayBuffer): ExifResult | null {
  const view = new DataView(buffer);
  if (view.byteLength < 4 || view.getUint16(0) !== 0xffd8) return null;
  let off = 2;
  while (off + 4 <= view.byteLength) {
    const marker = view.getUint16(off);
    const segLen = view.getUint16(off + 2);
    if (marker === 0xffe1 && off + 10 <= view.byteLength) {
      const e = view.getUint8(off + 4);
      const x = view.getUint8(off + 5);
      const i = view.getUint8(off + 6);
      const f = view.getUint8(off + 7);
      if (e === 0x45 && x === 0x78 && i === 0x69 && f === 0x66) {
        const tiffStart = off + 10;
        const tiffLen = segLen - 8;
        if (tiffStart + tiffLen > view.byteLength) return null;
        const tv = new DataView(buffer, tiffStart, tiffLen);
        const bo = tv.getUint16(0);
        const le = bo === 0x4949;
        if (tv.getUint16(2, le) !== 42) return null;
        const ifd0Off = tv.getUint32(4, le);
        const result: ExifResult = {};
        const { exifIFD, gpsIFD } = readIFD(tv, ifd0Off, le, IFD0_TAGS, result);
        if (exifIFD !== undefined) readIFD(tv, exifIFD, le, EXIF_TAGS, result);
        if (gpsIFD  !== undefined) readIFD(tv, gpsIFD,  le, GPS_TAGS,  result, true);
        return Object.keys(result).length > 0 ? result : null;
      }
    }
    if ((marker & 0xff00) !== 0xff00 || segLen < 2) break;
    off += 2 + segLen;
  }
  return null;
}

// ── Formatting helpers ────────────────────────────────────────────────────

function formatTagValue(tag: string, val: string | number): string {
  if (typeof val === "string") return val;
  if (tag === "FNumber") return `f/${val.toFixed(1)}`;
  if (tag === "ExposureTime") return val < 1 ? `1/${Math.round(1 / val)}s` : `${val}s`;
  if (tag === "FocalLength") return `${val.toFixed(1)}mm`;
  if (tag === "Flash") return val === 0 ? "No flash" : val === 1 ? "Flash fired" : `0x${val.toString(16)}`;
  if (tag === "Orientation") {
    return (["", "Normal", "Flip H", "180°", "Flip V", "Transpose", "90° CW", "Transverse", "270° CW"])[val as number] ?? String(val);
  }
  if (tag === "ResolutionUnit") return val === 2 ? "inch" : val === 3 ? "cm" : String(val);
  if (tag === "ExposureProgram") {
    return (["Not defined", "Manual", "Normal", "Aperture priority", "Shutter priority", "Creative", "Action", "Portrait", "Landscape"])[val as number] ?? String(val);
  }
  if (tag === "MeteringMode") {
    return (["Unknown", "Average", "Center-weighted", "Spot", "Multi-spot", "Multi-segment", "Partial"])[val as number] ?? String(val);
  }
  if (tag === "ColorSpace") return val === 1 ? "sRGB" : val === 65535 ? "Uncalibrated" : String(val);
  if (tag === "GPSAltitudeRef") return val === 0 ? "Sea level" : "Below sea level";
  if (typeof val === "number") return Number.isInteger(val) ? String(val) : val.toFixed(4);
  return String(val);
}

type GroupKey = "camera" | "datetime" | "image" | "exposure" | "gps";

const TAG_GROUP: Record<string, GroupKey> = {
  Make: "camera", Model: "camera", Software: "camera", Artist: "camera",
  DateTime: "datetime", DateTimeOriginal: "datetime", DateTimeDigitized: "datetime",
  XResolution: "image", YResolution: "image", ResolutionUnit: "image",
  PixelXDimension: "image", PixelYDimension: "image", Orientation: "image",
  ColorSpace: "image", ExifVersion: "image",
  ExposureTime: "exposure", FNumber: "exposure", ISOSpeedRatings: "exposure",
  FocalLength: "exposure", Flash: "exposure", ExposureProgram: "exposure",
  ShutterSpeedValue: "exposure", ApertureValue: "exposure",
  ExposureBiasValue: "exposure", MeteringMode: "exposure",
  GPSLatitude: "gps", GPSLatitudeRef: "gps", GPSLongitude: "gps",
  GPSLongitudeRef: "gps", GPSAltitude: "gps", GPSAltitudeRef: "gps",
  GPSDateStamp: "gps",
};

const GROUP_ORDER: GroupKey[] = ["camera", "datetime", "image", "exposure", "gps"];

function dmsToDecimal(dms: string, ref: string): number | null {
  const m = dms.match(/([\d.]+)°\s*([\d.]+)'\s*([\d.]+)"/);
  if (!m) return null;
  const dec = parseFloat(m[1]) + parseFloat(m[2]) / 60 + parseFloat(m[3]) / 3600;
  return (ref === "S" || ref === "W") ? -dec : dec;
}

// ── Component ─────────────────────────────────────────────────────────────

export default function ExifViewer() {
  const t = useTranslations("exif-viewer");
  const [exif, setExif] = useState<ExifResult | null>(null);
  const [fileName, setFileName] = useState("");
  const [error, setError] = useState<"notJpeg" | "noExif" | null>(null);
  const [copiedTag, setCopiedTag] = useState<string | null>(null);
  const [copiedAll, setCopiedAll] = useState(false);
  const [dragging, setDragging] = useState(false);

  function processFile(file: File) {
    if (!file.type.includes("jpeg") && !file.type.includes("jpg") && !file.name.toLowerCase().endsWith(".jpg") && !file.name.toLowerCase().endsWith(".jpeg")) {
      setError("notJpeg"); setExif(null); setFileName(file.name); return;
    }
    setFileName(file.name);
    setError(null);
    const reader = new FileReader();
    reader.onload = (e) => {
      const buf = e.target?.result as ArrayBuffer;
      const result = parseExif(buf);
      if (!result) { setError("noExif"); setExif(null); }
      else { setExif(result); setError(null); }
    };
    reader.readAsArrayBuffer(file);
  }

  const handleDrop = useCallback((e: React.DragEvent) => {
    e.preventDefault(); setDragging(false);
    const file = e.dataTransfer.files[0];
    if (file) processFile(file);
  }, []);

  async function copyTag(tag: string, val: string) {
    await navigator.clipboard.writeText(val);
    setCopiedTag(tag); setTimeout(() => setCopiedTag(null), 2000);
  }

  async function copyAll() {
    if (!exif) return;
    await navigator.clipboard.writeText(JSON.stringify(exif, null, 2));
    setCopiedAll(true); setTimeout(() => setCopiedAll(false), 2000);
  }

  // GPS マップリンク生成
  let mapsUrl: string | null = null;
  if (exif?.GPSLatitude && exif?.GPSLongitude) {
    const lat = dmsToDecimal(String(exif.GPSLatitude), String(exif.GPSLatitudeRef));
    const lng = dmsToDecimal(String(exif.GPSLongitude), String(exif.GPSLongitudeRef));
    if (lat !== null && lng !== null) {
      mapsUrl = `https://www.google.com/maps?q=${lat.toFixed(6)},${lng.toFixed(6)}`;
    }
  }

  // グループ分け
  const grouped: Partial<Record<GroupKey, Array<{ tag: string; val: string }>>> = {};
  if (exif) {
    for (const [tag, raw] of Object.entries(exif)) {
      const group = TAG_GROUP[tag] ?? "camera";
      if (!grouped[group]) grouped[group] = [];
      grouped[group]!.push({ tag, val: formatTagValue(tag, raw) });
    }
  }

  return (
    <div className="space-y-4">
      {/* ドロップゾーン */}
      <label
        onDragOver={(e) => { e.preventDefault(); setDragging(true); }}
        onDragLeave={() => setDragging(false)}
        onDrop={handleDrop}
        className={`flex flex-col items-center justify-center border-2 border-dashed rounded-xl
                    p-8 cursor-pointer transition-colors ${
                      dragging ? "border-primary bg-primary/5" : "border-gray-300 hover:border-primary/50"
                    }`}
      >
        <input
          type="file"
          accept="image/jpeg,image/jpg,.jpg,.jpeg"
          className="sr-only"
          onChange={(e) => { const f = e.target.files?.[0]; if (f) processFile(f); }}
        />
        <span className="text-3xl mb-2">📷</span>
        <p className="text-sm font-medium text-gray-700">{t("dropLabel")}</p>
        <p className="text-xs text-gray-400 mt-1">{t("browseLabel")}</p>
      </label>

      {fileName && (
        <p className="text-xs text-gray-500 text-center">📄 {fileName}</p>
      )}

      {error === "notJpeg" && (
        <p className="text-sm text-center text-amber-600 bg-amber-50 rounded-lg py-3 px-4">
          {t("notSupported")}
        </p>
      )}
      {error === "noExif" && (
        <p className="text-sm text-center text-gray-500 bg-gray-50 rounded-lg py-3 px-4">
          {t("noExif")}
        </p>
      )}

      {exif && (
        <>
          <div className="flex justify-end">
            <button
              type="button"
              onClick={copyAll}
              className="text-xs px-3 py-1.5 rounded bg-gray-200 hover:bg-gray-300 text-gray-700 transition-colors"
            >
              {copiedAll ? t("copied") : t("copyAll")}
            </button>
          </div>

          {GROUP_ORDER.map((group) => {
            const items = grouped[group];
            if (!items?.length) return null;
            return (
              <div key={group} className="tool-card overflow-hidden">
                <div className="px-4 py-2 bg-gray-50 border-b border-gray-100">
                  <h2 className="text-xs font-semibold text-gray-500 uppercase tracking-wide">
                    {t(`groups.${group}`)}
                  </h2>
                </div>
                <div className="divide-y divide-gray-100">
                  {items.map(({ tag, val }) => (
                    <div key={tag} className="flex items-center justify-between px-4 py-2.5 hover:bg-gray-50 gap-3">
                      <span className="text-xs text-gray-500 w-36 shrink-0">{tag}</span>
                      <span className="text-sm font-mono flex-1 truncate">{val}</span>
                      <button
                        type="button"
                        onClick={() => copyTag(tag, val)}
                        className="text-xs px-2 py-0.5 rounded bg-gray-100 hover:bg-gray-200 text-gray-600 transition-colors shrink-0"
                      >
                        {copiedTag === tag ? "✓" : t("copy")}
                      </button>
                    </div>
                  ))}
                  {group === "gps" && mapsUrl && (
                    <div className="px-4 py-2.5">
                      <a
                        href={mapsUrl}
                        target="_blank"
                        rel="noopener noreferrer"
                        className="text-xs text-primary hover:underline"
                      >
                        🗺️ {t("mapsLink")}
                      </a>
                    </div>
                  )}
                </div>
              </div>
            );
          })}
        </>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 日本語翻訳ファイル作成**

`src/messages/ja/tools/exif-viewer.json`:

```json
{
  "title": "画像EXIFデータ閲覧ツール",
  "description": "JPEG画像のEXIFメタデータ（カメラ機種・撮影日時・GPS座標・露出設定）をブラウザ上で確認できる無料ツール。外部送信なし。",
  "keywords": [
    "EXIF データ 確認 オンライン",
    "画像 メタデータ 閲覧",
    "写真 撮影日時 確認",
    "EXIF GPS 座標",
    "カメラ情報 確認 無料",
    "JPEG EXIF 読み込み"
  ],
  "dropLabel": "JPEGファイルをドラッグ&ドロップ",
  "browseLabel": "またはクリックしてファイルを選択",
  "noExif": "この画像にはEXIFデータが含まれていないか、対応形式ではありません。",
  "notSupported": "JPEG（.jpg / .jpeg）ファイルのみ対応しています。",
  "copyAll": "全データをJSONでコピー",
  "copy": "コピー",
  "copied": "コピー済み",
  "mapsLink": "Google Mapsで撮影場所を確認",
  "groups": {
    "camera": "カメラ情報",
    "datetime": "撮影日時",
    "image": "画像情報",
    "exposure": "露出設定",
    "gps": "GPS情報"
  },
  "howToUse": [
    {
      "label": "JPEG画像をドロップする",
      "description": "EXIFデータを確認したいJPEG画像（.jpg / .jpeg）をドロップエリアにドラッグするか、クリックしてファイルを選択します。"
    },
    {
      "label": "EXIFデータを確認する",
      "description": "カメラ情報・撮影日時・画像情報・露出設定・GPS情報の5グループに分けて表示されます。GPS座標があればGoogle Mapsリンクが表示されます。"
    },
    {
      "label": "データをコピーする",
      "description": "各タグ行のコピーボタンで値を個別にコピーするか、「全データをJSONでコピー」で全EXIFデータをJSON形式でコピーできます。"
    }
  ],
  "faq": [
    {
      "question": "対応しているファイル形式は？",
      "answer": "JPEG（.jpg / .jpeg）形式のみ対応しています。PNG・WebP・HEICなどの形式はEXIF APP1セグメントの構造が異なるため対応していません。"
    },
    {
      "question": "撮影日時が表示されない場合は？",
      "answer": "一部のスマートフォンやSNSへアップロードした画像はEXIFデータが削除されていることがあります。また、スクリーンショットやデジカメ以外で撮影した画像にはEXIFが含まれない場合があります。"
    },
    {
      "question": "GPS情報が表示される条件は？",
      "answer": "スマートフォンで位置情報をオンにして撮影した写真にはGPS座標が含まれます。デジタルカメラはGPS非搭載のモデルが多いため表示されないことがあります。GPS情報があればGoogle Mapsリンクが自動生成されます。"
    },
    {
      "question": "画像はサーバーに送信されますか？",
      "answer": "いいえ。すべての処理はブラウザ上で完結します。画像ファイルが外部に送信されることは一切ありません。FileReader APIとDataView APIでローカルに解析しています。"
    },
    {
      "question": "EXIFデータを削除したい場合は？",
      "answer": "このツールはEXIFデータの「閲覧」専用です。SNS投稿前にEXIFを削除したい場合は、画像圧縮ツール（ブラウザのCanvas出力でEXIFが除去される）や専用のメタデータ削除ツールをお使いください。"
    }
  ]
}
```

- [ ] **Step 3: 英語翻訳ファイル作成**

`src/messages/en/tools/exif-viewer.json`:

```json
{
  "title": "EXIF Data Viewer",
  "description": "View EXIF metadata from JPEG images — camera model, date taken, GPS location, and exposure settings — directly in your browser. Free, no upload.",
  "keywords": [
    "exif viewer online",
    "jpeg metadata reader",
    "photo exif data free",
    "image metadata viewer",
    "exif gps location",
    "camera info from photo"
  ],
  "dropLabel": "Drop a JPEG file here",
  "browseLabel": "or click to browse",
  "noExif": "This image does not contain EXIF data, or the EXIF segment could not be parsed.",
  "notSupported": "Only JPEG (.jpg / .jpeg) files are supported.",
  "copyAll": "Copy all as JSON",
  "copy": "Copy",
  "copied": "✓",
  "mapsLink": "View location on Google Maps",
  "groups": {
    "camera": "Camera",
    "datetime": "Date & Time",
    "image": "Image Info",
    "exposure": "Exposure",
    "gps": "GPS"
  },
  "howToUse": [
    {
      "label": "Drop a JPEG image",
      "description": "Drag and drop a JPEG image onto the drop zone, or click to select a file."
    },
    {
      "label": "Review the EXIF data",
      "description": "Metadata is shown in five groups: Camera, Date & Time, Image Info, Exposure, and GPS. A Google Maps link is generated if GPS coordinates are present."
    },
    {
      "label": "Copy the data",
      "description": "Click Copy next to any tag to copy its value, or use 'Copy all as JSON' to copy all EXIF data at once."
    }
  ],
  "faq": [
    {
      "question": "What file formats are supported?",
      "answer": "Only JPEG (.jpg / .jpeg) files are supported. PNG, WebP, and HEIC files use different metadata structures and are not supported."
    },
    {
      "question": "Why is the date taken not showing?",
      "answer": "Some smartphones and social media platforms strip EXIF data when sharing images. Screenshots and non-camera images also typically lack EXIF data."
    },
    {
      "question": "When does GPS data appear?",
      "answer": "GPS data is embedded when a photo is taken with location services enabled on a smartphone. Most dedicated cameras lack GPS. When GPS data is present, a Google Maps link is generated automatically."
    },
    {
      "question": "Is my image uploaded to a server?",
      "answer": "No. All processing happens in your browser using the FileReader and DataView APIs. Your image is never sent to any server."
    },
    {
      "question": "Can I remove EXIF data with this tool?",
      "answer": "This tool is for viewing EXIF data only. To strip EXIF before sharing, use the Image Compressor tool (Canvas output removes EXIF) or a dedicated metadata-stripping tool."
    }
  ]
}
```

- [ ] **Step 4: toolsRegistry.ts に追加** (`countdown-timer` の後)

```ts
  {
    slug: "exif-viewer",
    category: "image",
    icon: "🔬",
    component: "ExifViewer",
    updatedAt: "2026-04-18",
  },
```

- [ ] **Step 5: toolComponents.tsx に追加** (`CountdownTimer` の後)

```tsx
  ExifViewer: dynamic(() => import("@/tools/ExifViewer"), { loading: Loading }),
```

- [ ] **Step 6: ビルド確認**

```bash
npm run build
```

Expected: ✓ Compiled successfully

- [ ] **Step 7: コミット**

```bash
git add src/tools/ExifViewer.tsx \
        src/messages/ja/tools/exif-viewer.json \
        src/messages/en/tools/exif-viewer.json \
        src/lib/toolsRegistry.ts \
        src/lib/toolComponents.tsx
git commit -m "[ツール追加] exif-viewer: 画像EXIFデータ閲覧ツール"
```

---

### Task 7: README 更新

**Files:**
- Modify: `README.md`

- [ ] **Step 1: README のツール一覧テーブルを更新**

「登録済みツール（52件）」→「登録済みツール（64件）」に変更し、B15/B16の12件を追記:

```
| 53 | `date-calculator` | 日付差計算機 | Date Difference Calculator | calculate |
| 54 | `calorie-calculator` | カロリー計算機 | Calorie Calculator | lifestyle |
| 55 | `body-fat-calculator` | 体脂肪率計算機 | Body Fat Calculator | lifestyle |
| 56 | `sleep-calculator` | 睡眠時間計算機 | Sleep Calculator | lifestyle |
| 57 | `css-box-shadow` | CSSボックスシャドウ生成 | CSS Box Shadow Generator | dev |
| 58 | `wcag-contrast-checker` | WCAGコントラスト比チェッカー | WCAG Contrast Checker | dev |
| 59 | `text-deduplicator` | 重複行削除ツール | Text Deduplicator | text |
| 60 | `character-counter-jp` | 原稿用紙換算・文字数カウンター | Japanese Character Counter | text |
| 61 | `number-formatter` | 数値フォーマッター | Number Formatter | convert |
| 62 | `char-frequency` | 文字頻度分析ツール | Character Frequency Analyzer | text |
| 63 | `countdown-timer` | カウントダウンタイマー | Countdown Timer | lifestyle |
| 64 | `exif-viewer` | 画像EXIFデータ閲覧 | EXIF Data Viewer | image |
```

- [ ] **Step 2: 「次にやること」を更新**

バッチ16を ✅ 完了としてマーク。バッチ17候補（次回）を追記。

- [ ] **Step 3: コミット**

```bash
git add README.md
git commit -m "[ドキュメント] README更新: バッチ16完了・登録ツール64件"
```
