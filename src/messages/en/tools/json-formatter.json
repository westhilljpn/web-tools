{
  "title": "JSON Formatter / Validator",
  "description": "Format, prettify, and validate JSON online. Supports 2-space, 4-space, and tab indentation. Minify JSON to a single line. Free online tool.",
  "keywords": [
    "JSON formatter",
    "JSON prettify",
    "JSON validator",
    "JSON minify",
    "JSON beautifier",
    "JSON lint"
  ],
  "inputLabel": "Input JSON",
  "outputLabel": "Output",
  "placeholder": "Paste or type your JSON here...",
  "outputPlaceholder": "Formatted JSON will appear here...",
  "modes": {
    "format": "Format",
    "minify": "Minify"
  },
  "indent": {
    "label": "Indent",
    "2": "2 spaces",
    "4": "4 spaces",
    "tab": "Tab"
  },
  "status": {
    "valid": "Valid JSON",
    "invalid": "Invalid JSON"
  },
  "stats": {
    "lines": "{count} lines",
    "bytes": "{size} bytes"
  },
  "buttons": {
    "paste": "📋 Paste",
    "copy": "Copy",
    "clear": "Clear",
    "sample": "Sample"
  },
  "toast": {
    "copied": "Copied!",
    "pasted": "Pasted from clipboard",
    "pasteError": "Clipboard access denied",
    "sampleLoaded": "Sample JSON loaded"
  },
  "howToUse": [
    {
      "label": "Paste your JSON",
      "description": "Click Paste to paste from clipboard, or type JSON directly in the input area. A sample is available to try the tool immediately."
    },
    {
      "label": "Choose a mode",
      "description": "Select Format to prettify the JSON with readable indentation, or Minify to compress it into a single line."
    },
    {
      "label": "Adjust indentation",
      "description": "In Format mode, choose 2 spaces, 4 spaces, or Tab as the indentation style."
    },
    {
      "label": "Check validation and copy",
      "description": "A green indicator confirms valid JSON. If the JSON is invalid, the error message shows what went wrong. Copy the output with the Copy button."
    }
  ],
  "faq": [
    {
      "question": "My JSON shows an error. What are the most common causes?",
      "answer": "The most common JSON errors are: (1) trailing commas after the last item in an array or object, (2) single-quoted strings instead of double quotes, (3) unquoted property keys, (4) missing or mismatched brackets/braces, and (5) using JavaScript-only values like undefined or NaN. The validator highlights the line and position of the error."
    },
    {
      "question": "Can I paste an API response directly?",
      "answer": "Yes. Paste the raw response body from any REST API, browser DevTools Network tab, curl output, or Postman and click Format. If the API returns a double-encoded JSON string (JSON wrapped inside another string), strip the outer quotes first."
    },
    {
      "question": "Does it support JSON5 or JSON with comments?",
      "answer": "No. This tool validates standard JSON (RFC 8259) only. JSON5, JavaScript object literals, and JSONC (JSON with comments) are not supported. Remove any comments or trailing commas before pasting."
    },
    {
      "question": "Is my JSON data sent to a server?",
      "answer": "No. All processing is done entirely in your browser using the built-in JSON.parse and JSON.stringify functions. Your data never leaves your device."
    }
  ]
}
